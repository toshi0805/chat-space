.sidebar
  .sidebar__header
    .header__name 
      .left__child
        ユーザーネーム  
      .right__child
        .edit__cog
          = link_to new_group_path do
            %i.fas.fa-edit
            = icon('fas', 'edit-square')
          = link_to edit_user_path(current_user) do
            %i.fas.fa-cog
            = icon('fas', 'cog-square')
        
 
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            新着メッセージ
            
            


